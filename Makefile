
# Install the deps
i install:
	npm install

# The Run command for the server
r run:
	swift run

# Compile Assets Once
wd webpack-dev:
	npm run dev

# Compile Assets on every change
ww webpack-watch:
	npm run watch